// Stub P0-1 — pipeline asset thật (gltf-transform: Meshopt + KTX2 + resize texture)
// vào ở P0-3. Giữ exit 0 để `vercel-build` và `npm run ci` chạy trọn chuỗi
// `assets:build → vite build → copy legacy` ngay từ bây giờ.
console.log("[assets:build] stub P0-1 — chưa có asset để xử lý (pipeline thật vào ở P0-3).");
